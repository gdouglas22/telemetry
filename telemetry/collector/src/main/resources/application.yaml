grpc:
  server:
    port: 59091

spring:
  application:
    name: collector

collector:
  kafka:
    bootstrap-servers: localhost:9092
    topics:
      sensors: telemetry.sensors.v1
      hubs: telemetry.hubs.v1
